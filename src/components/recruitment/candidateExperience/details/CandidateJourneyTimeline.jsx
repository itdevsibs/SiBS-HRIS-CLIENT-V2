import { OutcomeBadge } from "../presentation.jsx";
import { formatExperienceDate } from "../presentation.jsx";
export default function CandidateJourneyTimeline({ record }) { const items = record.stageTimeline || []; return <section className="rounded-xl border border-[#E6ECF2] bg-white p-4"><div className="flex items-center justify-between gap-3"><div><h3 className="text-xs font-black uppercase tracking-wider text-[#042C51]">Recruitment Outcome & Journey</h3><p className="mt-1 text-[11px] text-slate-500">Final stage: <strong className="text-[#042C51]">{record.finalStage || "—"}</strong></p></div><OutcomeBadge outcome={record.outcome} /></div>{record.outcome === "Drop-off" ? <div className="mt-3 rounded-xl border border-rose-100 bg-rose-50 p-3"><p className="text-[9px] font-black uppercase text-rose-500">Drop-off Reason</p><p className="mt-1 text-xs font-bold text-rose-700">{record.dropOffCategory || "Unspecified"}</p><p className="mt-1 text-xs leading-5 text-rose-700/80">{record.dropOffReason || "No drop-off reason recorded."}</p></div> : null}<div className="mt-4 space-y-2">{items.length ? items.map((item, index) => <div key={item.id || `${item.stage}-${index}`} className="flex gap-3"><div className="flex flex-col items-center"><span className="flex h-7 w-7 items-center justify-center rounded-full bg-[#042C51] text-[10px] font-black text-white">{index + 1}</span>{index < items.length - 1 ? <span className="min-h-6 w-px flex-1 bg-slate-200" /> : null}</div><div className="mb-2 flex flex-1 items-center justify-between gap-3 rounded-xl bg-[#F8FAFC] p-3"><div><p className="text-xs font-black text-[#344054]">{item.stage}</p><p className="mt-0.5 text-[10px] text-slate-400">{formatExperienceDate(item.date)}</p></div><span className="rounded-full bg-white px-2 py-1 text-[9px] font-bold text-slate-600">{item.status}</span></div></div>) : <div className="rounded-xl border border-dashed border-slate-200 bg-slate-50 p-5 text-center text-xs text-slate-400">No candidate journey timeline recorded.</div>}</div></section>; }
